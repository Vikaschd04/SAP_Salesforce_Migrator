public with sharing class OrderController {
    // [mock] deterministic stub — replace by running with a real provider.
    public static List<Object> execute(List<Object> records) {
        if (records == null) { return new List<Object>(); }
        return records;
    }
}