[pytest]
# `slow` marks the product-level golden regression, which runs two full migrations.
# Kept in the default run on purpose — it is the check that protects the shipped
# pipeline, and a guard nobody runs is not a guard. Skip it during tight loops with:
#     pytest -m "not slow"
markers =
    slow: full end-to-end migration; ~30s each
